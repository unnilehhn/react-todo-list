import React from 'react'

var App = React.createClass ({
    render() {
        return 
            <div> This is definitely a React app now! </div>
    }
})

// allows us to import this to client.js
export default App

 
// fjern () fra return hvis koden ikke funker 